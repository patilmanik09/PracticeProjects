package InterfacePackage;

public interface InterfaceClass {
	
//In interface we cannot change the variables and its is special class which have only method declaration they don't have body 
    final int x=10;
	void add(int param1, int param2);
    void sub(int a, int b);

}
